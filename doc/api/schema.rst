Schema
======
TBD


Query
^^^^^
URL it's accessed by whether it's visible to public.  Location of code for this app.

Example query curl command


Header
^^^^^^
URL it's accessed by whether it's visible to public.  Location of code for this app.

Example query curl command




Download Single
^^^^^^^^^^^^^^^
URL it's accessed by whether it's visible to public.  Location of code for this app.
Note it's rate limited 
Example query curl command



Download Multiple
^^^^^^^^^^^^^^^^^
URL it's accessed by whether it's visible to public.  Location of code for this app.
Note that it streams a gzipped tarball, is rate limited.
Example query curl command


Login
^^^^^
URL it's accessed by whether it's visible to public.  Location of code for this app.
Note that it streams a gzipped tarball, is rate limited.
Example query curl command
